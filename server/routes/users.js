import express from 'express';
const router = express.Router();


router.route('/:id').put( );

router.route('/:id').delete( );

router.route('/:id').get( );

router.route('/').get( );


export default router;